#!/usr/bin/env python

from setuptools import setup

setup(name='toolbox',
      version='1.0',
      description='General use tool box',
      author='cabrittin',
      author_email='cabrittin at the google',
      url='git@github.com:cabrittin/toolbox.git',
      packages=['toolbox'],
     )
